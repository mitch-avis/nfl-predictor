# Next Agent Session Prompt

You are resuming work in the `nfl-predictor` workspace (`/home/mitch/workspace/nfl-predictor`).
Read `AGENTS.md` first and treat its delegation guardrails (rules 1-14) as binding, then
`.agents/TODO.md` (above all "Roadmap Status" and Milestone 60) and this file.

## State (written 2026-09-24, after task 55.8 closed)

- The user is merging `feat/m55-8-season-weighting` into `main` and pushing it before this session
  starts. Verify it first: `git status`, `git log --oneline -5 main`, and that `main` carries
  `f8fbd09 chore(release): bump version to 0.18.0` (or a merge commit containing it). If the merge
  is not there, stop and ask; do not merge or push yourself (must-ask, rule 5).
- Version `0.18.0`. Run a plain `uv sync` once on `main` after the merge (the version bumped), then
  `scripts/gate.sh`. The last gate on the branch exited `0` (`889 passed`, coverage `92.45%`; the
  figure drifts by a few hundredths between runs).
- Task 55.8 is closed: production trains **unweighted** (`config/weekly_run.yaml` sets neither
  `train_recency_half_life_seasons` nor `wf_recency_half_life_seasons`; `tests/test_weekly_run.py`
  pins that). Record: `ARCHIVE.md` Milestone 55 "55.8", `AGENTS.md` "Season weighting and the
  six-season fit-noise floor". Roadmap step 1 is done once the merge lands.
- `models/` is gitignored; everything under it stays on disk. Never delete or overwrite anything
  there or under `data/` without asking.
- `.agents/gpt-5-4_task_55-8_transcript.md` is the user's own untracked file; leave it alone.

## Your task: roadmap step 2, Milestone 60 with task 55.5

CLI and entrypoint consolidation, widened to every file under `scripts/`, **behavior-preserving**.
The full plan, ground rules and acceptance criteria are Milestone 60 in `.agents/TODO.md`; task
55.5 (the `ScoreModel` fate) is decided and executed inside it.

1. Create the branch `feat/m60-cli-consolidation` off the merged `main` (rule 6 allows this).
2. **60.1 and 60.2 first: the inventories, generated by script** (rule 10). Put the generator
   script(s) under `.agents/m60/` and the generated inventory beside them. Per flag: every
   entrypoint's parser actions, defaults, help, whether `config/weekly_run.yaml` or the web job
   templates set it, and every consumer. Per file under `scripts/`: size, imports, importing
   tests, web templates, CI/gate calls, docs, and `models/*/launch.sh` callers. Classify every
   item with evidence. If any search or tool call fails or returns "No matches found" where matches
   must exist, report it as a failure; never fill the gap by inference.
   `.agents/m60_cli_flag_audit.md` is superseded and must not be used as a source. The findings
   already verified are listed under 60.1 and 60.2 in `TODO.md`: confirm or overturn each one with
   the script.
3. Include the 55.5 evidence (every use of `ScoreModel` and `--model-kind score`) and the "(step 2)"
   follow-ups from the assignment table under "Roadmap Status" in the same inventory.
4. **Then stop at 60.3.** Report the proposed dispositions for the user's sign-off: the removal
   list, the surviving canonical names, each script's fate, the command style for moved
   entrypoints, and the reproduction policy for old `scripts/<name>.py` paths in `models/*/launch.sh`.
   Nothing from 60.4 onward starts before that sign-off.
5. Commit the inventory chunk on the branch (Conventional Commits, one logical change per commit),
   and rewrite this file before you stop (rule 8).

Constraints that matter here:

- The weekly run must keep working every week while the milestone is open (it is mid-season).
  Moves are pinned by the weekly-run characterization test (60.4) before any code moves.
- No walk-forward runs are needed for this milestone. Edits under `nfl_predictor/ml/` change
  every checkpoint fingerprint, so do not start any reference run while ML code is moving.
- A behavior defect found along the way (for example the stage-1 list-order selection, task 56.5)
  is listed and left for its own step, never folded into a move.
- Changing any default, merging, pushing, and touching the web API on port 8765 are must-ask.

## Open questions for the user (carry forward)

- Delete the unreferenced checkpoint folder `models/wf_checkpoints/ca3e6892daacd42980f4/` (left
  by a stray default-config walk-forward on 2026-09-23)?
- Prune the stale `../nfl-predictor-web` worktree record, and where the web app will run from.
- How production probabilities should be formed (task 56.5, roadmap step 3).

## Notes

- The Week 3 weekly run can go whenever the user wants; its stage 1 now trains unweighted, so its
  fold checkpoints retrain from scratch. Do not run it yourself unless asked, and never alongside
  another walk-forward.
- `models/wf_m55_8_review/probability_paths.py` has no second key; none of its numbers go into
  the docs until task 56.5 rescores it independently.
