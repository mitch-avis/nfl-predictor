# Next Agent Session Prompt

You are resuming work in the `nfl-predictor` workspace (`/home/mitch/workspace/nfl-predictor`).
Read `AGENTS.md` first and treat its delegation guardrails (rules 1-14) as binding, then
`.agents/TODO.md` and this file.

## State (written 2026-09-24, task 55.8 closed)

- Branch `feat/m55-8-season-weighting` (also on `origin`, behind the local commits until pushed),
  version `0.18.0`. `main` is at the `0.16.2` merge.
- `scripts/gate.sh` exits `0` on the `0.18.0` tree (`889 passed`, coverage `92.45%`).
- Task 55.8 is closed. The user chose **unweighted** production training on 2026-09-24 after an
  independent review of nine six-season arms (`models/wf_m55_8_review/INDEPENDENT_REVIEW.md`,
  which governs; the producer's `REVIEW.md` there now carries a corrections section).
  `config/weekly_run.yaml` no longer sets `train_recency_half_life_seasons` or
  `wf_recency_half_life_seasons`; `tests/test_weekly_run.py` pins both as `None`.
- Records updated: `ARCHIVE.md` (Milestone 55, "55.8"), `AGENTS.md` ("Season weighting and the
  six-season fit-noise floor", rule 11's gap list), `CHANGELOG.md` `0.18.0`, `README.md`,
  `TODO.md`.
- `models/` is gitignored; the 55.8 run directories and `models/wf_m55_8_review/` stay on disk.

## Waiting on the user (must-ask)

1. **Merge `feat/m55-8-season-weighting` into `main`** and push. Then run a plain `uv sync` on
   `main`.
2. Delete the unreferenced checkpoint folder `models/wf_checkpoints/ca3e6892daacd42980f4/` (left by
   a stray default-config walk-forward on 2026-09-23, killed after about two minutes)?
3. Prune the stale `../nfl-predictor-web` worktree record, and where the web app will run from.

## Next steps

1. After the merge: roadmap step 2, **Milestone 60 with task 55.5**, in a new session on its own
   branch off `main` (full plan under Milestone 60 in `TODO.md`; behavior-preserving, pinned by a
   characterization test of the weekly run; inventories generated by script, rule 10).
2. The full order is under "Roadmap Status" in `TODO.md`.

## Notes

- The Week 3 weekly run is not blocked by anything running (no walk-forward is active). Its stage
  1 now trains unweighted, so its fold checkpoints retrain from scratch. It still submits the
  stage-1 list-order winner (task 56.5, step 3).
- `models/wf_m55_8_review/probability_paths.py` has **no second key**; none of its numbers go into
  the docs until task 56.5 rescores it independently.
- Open question for step 3: how production probabilities should be formed (task 56.5).
