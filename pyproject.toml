[build-system]
requires = ["hatchling>=1.30"]
build-backend = "hatchling.build"

[project]
name = "nfl-predictor"
version = "0.8.0"
description = "NFL game score and outcome prediction with a margin/total ML model, calibrated win probabilities, and tooling for Pick 'Em and Confidence Pools."
readme = "README.md"
requires-python = ">=3.14"
keywords = ["stats", "sports", "football", "machine learning"]
authors = [{ name = "Mitch Avis", email = "mitchavis@gmail.com" }]
license = { text = "MIT" }
dependencies = [
    "argon2-cffi",
    "beautifulsoup4",
    "coloredlogs",
    "fastapi",
    "joblib",
    "lxml",
    "nflreadpy",
    "numpy",
    "openpyxl",
    "optuna",
    "pandas",
    "polars",
    "pydantic-settings",
    "pyjwt",
    "python-multipart",
    "requests",
    "scikit-learn",
    "scipy",
    "sse-starlette",
    "uvicorn[standard]",
    "xgboost",
]
classifiers = [
    "Programming Language :: Python :: 3.14",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
]

[project.urls]
Homepage = "https://github.com/mitch-avis/nfl-predictor"

[project.optional-dependencies]
web = [
    "argon2-cffi",
    "fastapi",
    "pydantic-settings",
    "pyjwt",
    "python-multipart",
    "sse-starlette",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "colorama",
    "httpx",
    "pandas-stubs>=3.0.3.260530",
    "pyright",
    "pytest",
    "pytest-cov",
    "pytest-html",
    "pytest-metadata",
    "pytest-sugar",
    "pytest-xdist",
    "ruff",
    "ty",
]

[tool.hatch.build.targets.wheel]
packages = ["nfl_predictor"]

[tool.uv]
default-groups = ["dev"]
required-version = ">=0.11.21"

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["."]
extend-exclude = [
    ".venv",
    ".git",
    "__pycache__",
    ".pytest_cache",
    ".mypy_cache",
    "build",
    "dist",
    "web",
    # A separate git clone of agent skills, not part of this project.
    ".agents/skills",
]

[tool.ruff.lint]
select = [
    "B",   # flake8-bugbear rules
    "C4",  # flake8-comprehensions rules
    "D",   # pydocstyle rules
    "E",   # pycodestyle errors
    "F",   # pyflakes rules
    "I",   # isort rules
    "N",   # pep8-naming rules
    "S",   # bandit security rules
    "SIM", # flake8-simplify rules
    "UP",  # pyupgrade rules
    "W",   # pycodestyle warnings
    "NPY", # numpy docstring rules
    "PGH", # pygrep-hooks rules
]
ignore = [
    "D203", # Conflicts with D211 (no-blank-line-before-class)
    "D213", # Conflicts with D212 (multi-line-summary-first-line)
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "S105", "S106"] # asserts and fixture passwords are standard in pytest
"nfl_predictor/api/registry/*.py" = ["E501"]  # one column definition per line, descriptions included

[tool.ruff.lint.isort]
known-first-party = ["nfl_predictor"]

[tool.ruff.format]
docstring-code-format = true

[tool.ty.environment]
# Pin ty to the repo venv so local and CI checks resolve the same interpreter.
python = ".venv"
python-version = "3.14"

[tool.ty.src]
# Limit ty to the checked-in source roots we validate in CI.
include = ["nfl_predictor", "scripts", "tests"]

[tool.pyright]
pythonVersion = "3.14"
# Keep pyright on the repo's stable standard-mode baseline, then promote the specific diagnostics we
# rely on in CI and Pylance.
typeCheckingMode = "standard"
venvPath = "."
venv = ".venv"
pythonPlatform = "Linux"
stubPath = "stubs"
executionEnvironments = [{ root = "." }]
# `nfl-sos-ratings` is a gitignored symlink to the read-only reference repo; it is not part
# of this project's type-check surface. The rest of the list restores pyright's defaults.
exclude = ["nfl-sos-ratings", "web", "**/node_modules", "**/__pycache__", "**/.*"]
# --- Standard-mode diagnostics promoted to errors (also surfaced by Pylance) ----------
reportMissingImports = "error"
reportMissingModuleSource = "warning"
reportAttributeAccessIssue = "error"
reportAssignmentType = "error"
reportArgumentType = "error"
reportReturnType = "error"
reportCallIssue = "error"
reportIndexIssue = "error"
reportOperatorIssue = "error"
reportOptionalSubscript = "error"
reportOptionalMemberAccess = "error"
reportOptionalCall = "error"
reportOptionalIterable = "error"
reportOptionalContextManager = "error"
reportOptionalOperand = "error"
reportGeneralTypeIssues = "error"
reportMissingTypeStubs = false
reportUnknownVariableType = "none"
reportUnknownMemberType = "none"
reportUnknownArgumentType = "none"
reportUnknownParameterType = "none"
reportUnknownLambdaType = "none"

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
addopts = [
    "-ra",                       # show extra summary for (s)kipped, (x)failed
    "--strict-markers",
    "--cov=nfl_predictor",
    "--cov-report=term-missing",
]
filterwarnings = ["ignore::DeprecationWarning"]
pythonpath = ["."]

[tool.coverage.run]
branch = true
source = ["nfl_predictor"]
omit = ["config.py", "config-*.py", "*/config.py", "*/config-*.py"]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_empty = true
exclude_lines = ["pragma: no cover", "if __name__ == \"__main__\":"]
